apiVersion: v1
kind: Service
metadata:
  name: {{ include "logforwarder.fullname" . }}
  labels:
    {{- include "logforwarder.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "logforwarder.selectorLabels" . | nindent 4 }}
